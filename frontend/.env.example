# Base URL for the FastAPI backend. Leave unset in dev (Vite proxy handles it).
# Set to your deployed backend origin in production, e.g.:
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
